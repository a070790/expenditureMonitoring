<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>User page</title>
</head>
<body>
<div>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <input type="submit" value="Sign out">
    </form>
</div>

<h2>Добавление чека</h2>
<div>
    <form method="post">
        <h3>Данные покупки</h3>
        <p>
            <label for="name">Наименование покупки</label>
            <select name="name" id="name">
                <option value="кафе">кафе</option>
                <option value="яблоки">яблоки</option>
                <option value="футболка">футболка</option>
            </select>
        </p>
        <p>
            <label for="category">Категория покупки</label>
            <select name="category" id="category">
                <option value="продукты">продукты</option>
                <option value="одежда">одежда</option>
                <option value="развлечения">развлечения</option>
            </select>
        </p>
        <p>
            <input type="text" name="price" placeholder="цена" />
        </p>
        <p>
            <input type="text" name="quantity" placeholder="количество" />
        </p>
        <p>
            <input type="hidden" name="addPurchase" value="{{addPurchase}}" />
        </p>
        <button type="submit" name="addPurchase" value="true">Добавить покупку</button>
        <p></p>
        <p></p>

        <h3>Данные чека</h3>
        <p>
            <input type="hidden" name="checkId" value="{{chId}}" />
        </p>
        <p>
            <input type="hidden" name="wasSaved" value="{{wasSaved}}" />
        </p>
        <p>
            <input type="text" name="date" placeholder="дата" />
        </p>
        <p>
            <input type="text" name="sum" readonly="readonly" value="{{sum_}}" placeholder="сумма" />
        </p>
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit" name="wasSaved" value="true">Сохранить чек</button>
    </form>
</div>

</body>
</html>